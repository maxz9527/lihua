<%= form_for(@companyp,:html => {:mutlipart => true}) do |f| %>
  <% if @companyp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@companyp.errors.count, "error") %> prohibited this companyp from being saved:</h2>

      <ul>
      <% @companyp.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_field :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
